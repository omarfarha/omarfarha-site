// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Used to build absolute URLs for canonical links and social-share (Open
  // Graph) images. Update this if the domain ever changes.
  site: 'https://omarfarha.com',
});
